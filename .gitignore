# 秘密情報 (WiFiパスワード等) — config.example.h を元に各自作成
firmware/moshimo_sign/config.h

# 個人名・担当・実機の所在などのローカルメモ (公開しない)
CLAUDE.local.md

# Claude Code のローカル設定 (PC固有のパスが入る)
.claude/settings.local.json

# ビルド生成物
build/
dist/
*.bin
*.elf
*.map

.DS_Store
