# 秘密情報 (WiFiパスワード等) — config.example.h を元に各自作成
firmware/moshimo_sign/config.h

# 個人名・担当・実機の所在などのローカルメモ (公開しない)
CLAUDE.local.md

# Claude Code のローカル設定 (PC固有のパスが入る)
.claude/settings.local.json

# ビルド生成物
build/
dist/
*.bin
*.elf
*.map
# 自己アップデートで配布する .bin だけは例外 (手順: docs/firmware-release.md)
!firmware/moshimo_sign.bin

# 会議音声・議事録などの非公開資料 (個人名が入るため公開しない)
private/
*.m4a
*.mp3

.DS_Store
