# フィードバック受信箱

<!--
記録形式(1件ごとに以下のブロックをコピーして追記する)

## YYYY-MM-DD / 表示名 / 一言要約
- 状態: 未処理
- 原文:
  > (もらった言葉をそのまま)
- 文脈: (どこで・どんな流れで出た話か)

- 状態は 未処理 → 対応中 → 対応済み。
- 対応済みになったものは feedback/done.md に移す。
- 表示名はDiscordの表示名かイニシャル。本名は書かない。
-->

## 2026-08-07 / mugi21 / イベントのコメント投稿→選別→掲示板表示システム
- 状態: 対応中（画面は実装済み・共有サーバの選定待ち）
- 原文:
  > 登壇者と聴衆がいるイベントで、聴衆（写真だと参加者）がスマートフォンでホームページに
  > アクセスし、そのホームページで質問・コメント・つぶやきなどから、さすがにこれは本当に
  > 見せられないような不適切なコメントだけ省いて電光掲示板に表示するシステムを構想しています。
  > また、その中間地点で管理者のPCに得られたコメントのログを表示して、手動でもコメントを
  > 選別できるようにし、かつそのログは保存できるようにしたいです。
- 文脈: Discordでの依頼。ホワイトボードの構想図（参加者→Webアプリ→もくもくPC→LEDパネル、
  GAS + Firebase と記載）の写真つき。
- 実装したもの: `prototypes/event-comments/`
  - 参加者画面（投稿）／管理画面（選別・ログ保存）の2画面
  - 自動判定は**捨てずに理由を付けて未確認に積む**だけ。最終判断は必ず人
  - 承認済みを `COMMENTS_URL` 形式（1行1コメント）で出力。ログはCSV/JSON保存
- サーバ側も実装した（2026-08-07）: `tools/event-comments-gas/Code.gs` + `docs/event-comments.md`
  - スプレッドシートを保存先にした GAS Webアプリ。口は list / add / state / text の4つ
  - 承認・見送りはスクリプトプロパティの `ADMIN_KEY` を知っている人だけ
  - 管理画面にURLと鍵を入れると切り替わる。参加者用URLとQRもそこに出る
- 残っていること:
  - **デプロイは未実施**（Googleアカウントでの操作が必要なため管理者が行う）。
    手順は `docs/event-comments.md` の1〜6。
  - `COMMENTS_URL` への設定はファームウェア担当（`config.h` は git管理外）。
- 補足: 実機側は既に対応済み。`fetchComments()` が15秒ごとにプレーンテキストを取得し、
  `messages` の後ろに連結する（最大20件 = `MAX_COMMENTS`）。

## 2026-08-07 / mugi21 / 似顔絵(ドット絵)を実機に出したい
- 状態: 未処理（実機側が未対応）
- 原文:
  > 似顔絵を実機に出して欲しい
- 文脈: Discordでの依頼。`prototypes/self-portrait/` の自画像を実機に出したい。
- 実機に足りないもの:
  - ファームウェアは文字しか描けない（`drawGlyph` / `drawText` のみ）。
    ドット絵を出す口がなく、`playlist.json` にも画像を渡すフィールドがない。
  - 必要なのは `docs/playlist-spec.md`「将来の拡張 (v2予定)」の `frames`。
    静止画1枚でよいなら `frames` のうちアニメーションなし版だけでも足りる。
  - データ形式は `prototypes/draw/` のURL形式（64×32の1bitを base64url）が既にあるので、
    それをそのまま playlist に載せられると作る側の導線がつながる（お絵かき→URL→実機）。
  - 1bitなら 64×32÷8 = 256バイト。色は既存の `colorTop`/`colorScroll` と同じ指定でよさそう。
- 関連: 日没アニメの件も同じ `frames` 待ち。まとめて設計すると早い。

## 2026-08-07 / mugi21 / 日没時刻になったら日没アニメーションを自動再生したい
- 状態: 未処理（Web版は実装済み・実機側が未対応）
- 原文:
  > 日没時間になったら日が沈むアニメーションを電光掲示板に流して
- 文脈: Discordでの依頼。Webプロトタイプ `prototypes/sunset/` として実装し、日の入り時刻の計算
  （緯度経度から算出・通信なし）とアニメーション本体は動く状態にした。
- 実機に載せるために足りないもの:
  1. `playlist.json` v2 の `frames`（ドット絵アニメーションの再生）— ファームウェア対応が必要
  2. `playlist.json` v2 の `schedule`（時刻で表示を切り替える）— ファームウェア対応が必要
  3. 実機側のRTC/NTP時刻合わせ。日の入り時刻の算出はESP32上でも計算できる（外部API不要）
- 判断: 1〜3はファームウェア担当の領域。playlist仕様v2の検討材料として残す。

## 2026-08-08 / mugi21 / もしもくんのアニメーションを通常営業の合間に流したい
- 状態: 未処理（Web版は実装済み・実機側が未対応）
- 原文:
  > いずれこの子達のアニメーションを通常営業で流れてるやつの合間に流したいな〜
- 文脈: `prototypes/moshimo-kun/` を作った流れで。もしもくん（ロゴの「も」「し」）の
  8種類のアニメーションを64×32で実装した。
- 実機に載せるために足りないもの:
  1. コマ送り再生。静止画1枚の `frames` では足りず、フレーム列＋1コマの表示時間＋
     ループ回数の指定がいる。
  2. 通常表示への割り込みの指定（何分おきに何秒だけ挟むか）。`messages` の
     ローテーションとは別の軸なので、playlist側に居場所が必要。
  3. データの置き場。1コマ 64×32÷8 = 256バイト。1〜2秒のループで15〜30コマ = 4〜8KB。
     `playlist.json` に直接載せると毎分の取得が重くなるので、フレームは別ファイル参照に
     するのが現実的。
- 実機で計算させる案について: 今回の描き方（距離場のsoft-min）は1ピクセルごとに
  exp/log を数十回使うので、ESP32で毎フレーム計算するのは重い。
  **コマを事前に焼いて送る**ほうが現実的。
- 判断: ファームウェア担当の領域。似顔絵・日没アニメの件と同じ待ちなので、
  playlist v2 のコマ送り仕様としてまとめて設計するのが早い。

## 2026-08-08 / mugi21 / 実機の営業ステータスをWebページから切り替えたい
- 状態: 未処理（Web側から書き込む口がない）
- 原文:
  > ステータスをHP上でいじれるようにしたい！
  > （もしもくんの体つきの話かを確認したうえで）Aのつもりだったけどどっちもあると便利だね！
- 文脈: `prototypes/moshimo-kun/` にステータス（体つき）のスライダーを付けた流れで、
  掲示板側の「営業中」などのステータスもWebから変えたい、という話が出た。
- できること・できないこと:
  - GitHub Pages は静的配信なので、ページから直接 `playlist.json` は書き換えられない。
    書き込みには GitHub 側の権限（トークン）が要る。
  - 安全にやるなら event-comments と同じ形。GASを挟んで鍵を知っている人だけが叩ける口を作り、
    GAS から GitHub API で `playlist.json` を更新する。トークンはGASのスクリプトプロパティに置く
    （リポジトリには絶対に置かない）。
  - すぐ作れる中間案: ステータスを選ぶと **`playlist.json` に貼る内容＋プレビューURL** を出すページ。
    Discordに貼ってもらえれば、こちらがcommitして1〜2分で実機に反映される。
- 次にやるなら: 中間案を先に作るのが早い。GAS経由の自動反映は鍵の管理方針を決めてから。
